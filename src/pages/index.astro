---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PostCard from '../components/PostCard.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const [featured, ...rest] = posts;
---

<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>

  <body>
    <Header />

    <main>
      <section class="hero">
        <div class="hero__inner">
          <p class="hero__kicker mono">BrowserMan · Writing</p>
          <h1 class="hero__title">
            AI agent browser automation.
          </h1>
          <p class="hero__lede">
            Guides, product updates, and practical notes for teams using AI agents to automate real browser workflows.
          </p>
        </div>
      </section>

      <section class="feed">
        <div class="feed__head">
          <h2 class="mono feed__label">Latest</h2>
          <span class="feed__count mono">{posts.length} {posts.length === 1 ? 'post' : 'posts'}</span>
        </div>

        {featured && <PostCard post={featured} featured />}
        {rest.map((post) => <PostCard post={post} />)}

        {posts.length === 0 && (
          <p class="feed__empty">
            Nothing published yet. The first post is on its way.
          </p>
        )}
      </section>
    </main>

    <Footer />
  </body>
</html>

<style>
  .hero {
    max-width: var(--wide);
    margin: 0 auto;
    padding: 4.25rem 1.5rem 2.75rem;
    border-bottom: 1px solid var(--rule);
  }

  .hero__inner { max-width: 58ch; }

  .hero__kicker {
    font-family: var(--mono);
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--amber-deep);
    margin: 0 0 1.2rem;
  }

  .hero__title {
    font-family: var(--serif);
    font-size: clamp(2.45rem, 5.4vw, 4rem);
    line-height: 1.04;
    letter-spacing: -0.02em;
    color: var(--ink-1);
    margin: 0 0 1rem;
    max-width: 14ch;
  }

  .hero__title em {
    font-style: italic;
    color: var(--ink-2);
  }

  .hero__lede {
    font-family: var(--serif);
    font-style: italic;
    font-size: 1.22rem;
    line-height: 1.5;
    color: var(--ink-2);
    margin: 0;
    max-width: 44ch;
  }

  .feed {
    max-width: var(--wide);
    margin: 0 auto;
    padding: 3rem 1.5rem 4rem;
  }

  .feed__head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 1rem;
  }

  .feed__label,
  .feed__count {
    font-family: var(--mono);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ink-3);
    font-weight: 500;
    margin: 0;
  }

  .feed__empty {
    font-family: var(--serif);
    font-style: italic;
    font-size: 1.2rem;
    color: var(--ink-3);
    text-align: center;
    padding: 4rem 0;
  }

  @media (max-width: 720px) {
    .hero { padding: 3rem 1rem 2rem; }
    .feed { padding: 2rem 1rem 3rem; }
  }
</style>
